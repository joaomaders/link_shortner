# link_shortner
 
#### List all saved urls
```/api/urls```

#### Access by short url
```/api/url/access/short_url```

#### Store new Url
```/api/url/store```

#### Check if short url isn't already used
```/api/url/exists/{short_url}```
